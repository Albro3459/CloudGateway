{
  "name": "cloudgateway-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Firestore security-rules tests run against the Firebase emulator.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.5.0",
    "firebase-tools": "^15.22.4",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
